%h1 Listing jobs

%table
  %tr
    %th
    %th
    %th

  - @jobs.each do |job|
    %tr
      %td= job.id
      %td= job.job_name
      %td= link_to 'Show', job
      %td= link_to 'Edit', edit_admin_job_path(job)
      %td= link_to 'Destroy', [:admin,job], :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Job', new_admin_job_path
